#include "lists.h"

/**
 * insert_dnodeint_at_index - adds a new node at a specific place.
 * @h: pointer to list's head
 * @idx: where the new node should be added in the list's index
 * @n: The new node's data
 * 
 * Return:  the new node's address, or NULL if it failed
 */

dlistint_t *insert_dnodeint_at_index(dlistint_t **h, unsigned int idx, int n)
{
}
